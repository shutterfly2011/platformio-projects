#pragma once

// Copy this file to Secrets.h (gitignored — never commit real credentials)
// and fill in your own values before building the "datacollector" env.

constexpr const char* kWifiSsid = "your-wifi-ssid";
constexpr const char* kWifiPassword = "your-wifi-password";

// Edge Impulse Studio -> your project -> Dashboard -> Keys -> API keys.
// Needs ingestion ("Data acquisition" / api-key) permission.
constexpr const char* kEdgeImpulseApiKey = "ei_xxxxxxxxxxxxxxxxxxxxxxxxxxxx";
